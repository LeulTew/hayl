{
  "name": "hayl-root",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test"
  },
  "devDependencies": {
    "autoprefixer": "10.4.23",
    "bun-types": "latest",
    "postcss": "8.5.6",
    "tailwindcss": "4.1.18"
  }
}
